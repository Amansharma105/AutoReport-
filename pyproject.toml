[project]
name = "autoreport"
version = "0.1.0"
description = "Automated report generation tool"

dependencies = [
    "typer",
    "pandas",
    "matplotlib",
    "seaborn",
    "jinja2",
    "reportlab",
    "apscheduler"
     "pytest"
]
